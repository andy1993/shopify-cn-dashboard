# ─── Shopify CN Dashboard — 环境变量模板 ───────────────────────────
# 将此文件复制为 .env 或 .env.local 并填入实际值。
# cp .env.example .env.local
#
# 注意：.env 和 .env.local 已被 .gitignore 排除，不会被提交到 Git 仓库。

# ── 汇率配置 ──────────────────────────────────────────────────────
# 美元兑人民币汇率，用于将 Shopify 订单金额换算为人民币。
# 默认为 7.25（2026 年参考汇率），可通过此变量自定义。
# 示例：
#   USD_CNY_RATE=7.28
USD_CNY_RATE=7.25

# ── Next.js 运行时配置（可选）──────────────────────────────────────
# 生产环境建议设置以下变量：
#   NODE_ENV=production

# ── DeepSeek AI 诊断（可选）─────────────────────────────────────────
# 激活 AI 实时诊断功能（对话式运营助手、智能分析报告）
# 注册获取 Key: https://platform.deepseek.com
# DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxx

# ── Shopify Admin API Token 所需权限 ───────────────────────────────
# 创建 Shopify 自定义应用时，需授权以下 Admin API scopes：
#   read_orders, write_orders              — 订单查询与管理
#   read_products, write_products           — 商品查询与编辑
#   read_customers                          — 客户数据读取
#   read_content, write_content             — 页面/博客/导航管理
#   read_inventory, write_inventory         — 库存读取与调整
#   read_locations                          — 仓库位置读取
#   read_shipping                           — 运费规则读取
#   read_markets                            — 多市场数据读取
#   read_translations, write_translations   — 翻译管理
